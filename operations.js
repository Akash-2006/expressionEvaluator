export const add = (number1, number2) => number1 + number2;

export const difference = (number1, number2) => number1 - number2;

export const multiplication = (multiplcand, multipler) => multiplcand * multipler;

export const division = (dividend, divisor) => dividend / divisor;

export const modularDivision = (dividend, divisor) => dividend % divisor;
